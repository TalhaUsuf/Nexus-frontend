# Backend Configuration
PORT=3001
NODE_ENV=development
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Microsoft OAuth Configuration
MICROSOFT_CLIENT_ID=your-microsoft-app-client-id
MICROSOFT_CLIENT_SECRET=your-microsoft-app-client-secret
MICROSOFT_TENANT_ID=common

# Email Configuration (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@nexus.com

# Database Configuration (for future use)
DATABASE_URL=postgresql://username:password@localhost:5432/nexus_db

# API Endpoints Documentation
# Authentication Endpoints:
# POST /api/auth/microsoft - Initiate Microsoft OAuth flow
# POST /api/auth/callback - Handle OAuth callback
# POST /api/auth/login - User login with email/password
# POST /api/auth/invite-login - Login via email invitation

# User Management Endpoints:
# GET /api/users - Get all users (admin only)
# POST /api/users/invite - Send user invitation (admin only)
# PUT /api/users/profile - Update user profile

# Bot Management Endpoints:
# GET /api/bot-approvals - Get bot approval requests (admin only)
# POST /api/bot-approvals/:id/approve - Approve/reject bot request (admin only)

# Chat Endpoints:
# POST /api/chat/message - Send chat message and get AI response
# GET /api/chat/reference/:id - Get detailed reference information

# Health Check:
# GET /api/health - Server health status